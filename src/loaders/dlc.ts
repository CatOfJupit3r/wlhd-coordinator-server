const DLCLoader = async () => {
    // After we receive information about the DLCs FROM GAME SERVERS, we load them here!
    // All DLCs are kept on git repositories on GitHub, so we can easily pull them here.
    // DLCLoader pulls only cosmetics from DLCs, not **Functions** (which are kept in the same git repository)
}

export default DLCLoader
